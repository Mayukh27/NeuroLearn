# ============================================================
# NEUROLEARN — Frontend Environment Variables
# ============================================================

# FastAPI Backend URL (no trailing slash)
# The frontend tries this first; falls back to local dummy data on failure
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Set to "true" to DISABLE fallback (errors if backend is down)
# NEXT_PUBLIC_API_STRICT=false
